[project]
name = "nba-2for1-analysis"
version = "0.1.0"
description = "An analysis on the two-for-one shot in the NBA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nba-api>=1.11.4",
    "numpy>=2.5.1",
    "pandas>=3.0.5",
    "pbpstats>=1.3.11",
    "pyarrow>=25.0.0",
]
